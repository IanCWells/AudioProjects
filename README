Welcome!

This repository is for audio and sound related projects.  

README.md will act as a general wiki for all things git, and list important references.  

To git clone:
cd path/to/your/directory
git clone https://github.com/example/repository.git
cd repository

For git add, commit, push:
(**Recommended to use virtual environment before this**)
$git add <filename>
$git commit -m <filename>
$git push

Push all files:
$git add .
$git commit -m "Your commit message here"
$git push origin master

To Edit in VSCode:
$cd AudioProjects 
$code .


Simple Low Pass and High Pass Filtering:
https://www.youtube.com/watch?v=Aht4letBAmA
